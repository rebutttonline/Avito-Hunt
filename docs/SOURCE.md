# Подключение источника объявлений

Avito Hunt намеренно не содержит скрытого скрейпера. Источник должен быть разрешённым, устойчивым и отдавать JSON по HTTPS. URL задаётся переменной `SOURCE_JSON_URL`.

Поддерживается массив объявлений или объект с массивом `items`:

```json
{
  "items": [
    {
      "id": "external-123",
      "title": "iPhone 15 Pro 256 ГБ",
      "price": 75000,
      "url": "https://example.test/items/123",
      "storage_gb": 256,
      "condition": "used",
      "status": "active",
      "region": "Москва",
      "published_at": "2026-08-17T10:00:00Z"
    }
  ]
}
```

Обязательные поля:

- `id` или `external_id` — постоянный уникальный идентификатор;
- `title` — заголовок с моделью iPhone;
- `price` — положительная цена в рублях;
- `url` — абсолютная HTTP(S)-ссылка.

Желательные поля: `storage_gb`, `condition`, `status`, `region`, `published_at`,
`description`, `battery_health` и стабильный идентификатор продавца. `status`
принимает `active`, `removed` или `sold` и позволяет учитывать снятие объявления.
Без этих полей точность группировки,
фильтрации и защиты от переопубликаций будет ниже.

До сохранения источник автоматически исключает обмен, сломанные устройства,
копии, аксессуары, запчасти и объявления с фиктивной ценой. Количество
исключений по каждой причине записывается в журнал без персональных данных.

Для продуктивной интеграции необходимо отдельно проверить условия использования источника, ограничения частоты запросов и допустимый способ обработки данных.

## Контракт адаптера

Внутри приложения любой поставщик преобразуется в `SourceBatch`: имя поставщика,
момент получения с часовым поясом, исходное количество записей, количество
отклонённых записей и нормализованные объявления. Перед обработкой контракт
проверяет уникальность внешних ID, положительную цену, абсолютный URL и корректные
временные метки. Поэтому смена лицензированного поставщика не затрагивает рыночный
анализ, Telegram-интерфейс и хранение данных.
